import { createStyles } from '@mantine/core';

export default createStyles((theme, _params, getRef) => ({
  image: {
    width: '100%',
    height: '100%',
    position: 'relative',
    objectFit: 'cover',
  },
}));
